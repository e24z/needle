{
  "schema": "needle.fixture_case.v1",
  "id": "read-visible-prune",
  "tool": "read",
  "artifact_kind": "file_text",
  "context_focus_question": "Where does load_config validate required keys?",
  "input": {
    "path": "demo/config_loader.py",
    "text": "import json\n\nDEFAULTS = {'timeout': 30, 'retries': 2}\n\ndef load_config(path):\n    raw = json.loads(open(path).read())\n    merged = {**DEFAULTS, **raw}\n    validate_required_keys(merged)\n    return merged\n\ndef validate_required_keys(config):\n    missing = [key for key in ('api_key', 'endpoint') if not config.get(key)]\n    if missing:\n        raise ValueError('missing config keys: ' + ', '.join(missing))\n\ndef render_banner(name):\n    return f'hello {name}'\n\ndef unused_fixture_alpha():\n    return 'alpha'\n\ndef unused_fixture_beta():\n    return 'beta'\n\ndef unused_fixture_gamma():\n    return 'gamma'\n"
  },
  "expected_behavior": "visible_prune",
  "assertions": {
    "chars_removed_gt": 0,
    "returned_contains": [
      "def load_config",
      "validate_required_keys"
    ],
    "returned_marks_filtered_lines": true
  }
}
